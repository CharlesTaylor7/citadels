# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json

id: use-anyhow-bail
message: Convert return Err().into() to bail!
severity: warning
language: rust
rule:
  pattern: |
    return Err($MSG.into());
fix:
  bail!($MSG)
